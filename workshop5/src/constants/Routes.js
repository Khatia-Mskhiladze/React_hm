const home = "/";
const universities = "/universities";
const notfound= "/universities/notfound";
const detailsPage = "/universities/:universityName";

const routes = {
    home,
    universities,
    notfound,
    detailsPage,
};

export default routes;

